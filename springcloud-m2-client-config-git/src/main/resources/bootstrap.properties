#profile and URL need to be in boostrap so that they get triggered before config service triggers
spring.profiles.active=default
#cloud config is poiting to server machine
spring.cloud.config.uri=http://localhost:8888
#credentials when use authorization is enabled on server
spring.cloud.config.username=pluralsight
spring.cloud.config.password=praveen
#When decryption is needed on client side
encrypt.key = ABCDEFGHI

